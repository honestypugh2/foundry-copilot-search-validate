# Default GA-only runtime dependencies for the Foundry + MCP agentic retrieval
# pattern. Preview Agent Framework deps live in requirements-agents-af.txt
# (TESTING ONLY — install in a separate venv with --prerelease=allow).
azure-functions==2.1.0
azure-identity>=1.25.3,<1.26
azure-ai-projects==2.2.0
azure-ai-contentunderstanding>=1.1.0,<1.2
azure-monitor-opentelemetry>=1.6.0,<2
azure-search-documents>=12.0.0,<12.1
azure-storage-blob>=12.29.0,<13
nest-asyncio>=1.6.0
openai>=2.41.0
pydantic>=2.13.4,<3
pypdf>=6.12.2
python-dotenv>=1.2.2
python-docx>=1.2.0

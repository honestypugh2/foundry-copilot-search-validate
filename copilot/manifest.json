{
    "$schema": "https://aka.ms/json-schemas/copilot-extensions/v1.0/declarative-copilot.schema.json",
    "name": "Knowledge Base Assistant",
    "description": "Answers questions grounded in the configured knowledge base.",
    "instructions": "You are a Knowledge Base Assistant. Answer questions using ONLY the documents retrieved from the knowledge base. Always cite the specific document and title. If information is not found, say so clearly.",
    "capabilities": [
        {
            "name": "AzureFunctionAction",
            "description": "Search the knowledge base",
            "endpoint": "${AZURE_FUNCTION_ENDPOINT}/api/ask",
            "method": "POST",
            "parameters": {
                "query": "{userMessage}"
            }
        }
    ]
}

# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: hr-policy-kb-lab
metadata:
  template: hr-policy-kb-lab
infra:
  provider: bicep
  path: ../infra
services:
  api:
    project: ./src/api
    language: python
    host: function
